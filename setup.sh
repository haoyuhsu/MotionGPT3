# Remember to use conda to install git-lfs with 'conda install -c conda-forge git-lfs'
# After that, run 'git lfs install' and 'git lfs pull' to download large files.
# The pretrained checkpoint of MotionGPT3 needs to be downloaded manually due to Google Drive's download limit.